<% if logged_in? %>
<%#    <div class="col-xs-8"><p>ログインユーザー:<%= current_user.name %></p>
  <h1>最新の投稿</h1>
  <%= render "posts/posts", posts: @posts %>
<% else %>
<div class="center jumbotron">
  <div class="text-center">
    <h1>Welcome to the Photo-Posts</h1>
    <% if @post %>
      <%= image_tag(@post.image, {size: 250}) %>
      <%#= @post.location %>
    <% end %>
    <%= link_to "SignUp", signup_path, class: "btn btn-lg btn-primary" %>
  </div>
</div>
<% end %>